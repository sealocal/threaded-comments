<header>
  <h1><%= "Comment ##{@comment.id}" %></h1>

  <p>This page shows a comment and all of its replies.</p>
  <p>Go back to the <%= link_to 'home page', root_path %> to see this top comment's parent comment.</p>
<header>

<main>


<p id="notice"><%= notice %></p>

<%= render @comment %>

</main>

<%= render 'shared/footer' %>
