<header>
  <h1>Listing Top Level Comments</h1>

  <p>This page is for demo purposes.  For most web app needs, there is no practical use case for an index of top-level comments.</p>
<header>

<main>

  <p id="notice"><%= notice %></p>
  <%= link_to 'New Comment', new_comment_path %>
  <% @comments.each do |comment| %>
    <div class="comment">
      <ul class="author">
        <li><%= comment.full_name %></li>
        <li>(<%= mail_to comment.email %>)</li>
      </ul>
      <p class="content">
        <%= comment.id %>
        Lorem ipsum
      </p>
      <ul class="actions">
        <li><%= link_to 'Show', comment %></li>
        <li><%= link_to 'Edit', edit_comment_path(comment) %></li>
        <li><%= link_to 'Destroy', comment, method: :delete, data: { confirm: 'Are you sure?' } %></li>
      </ul>
    </div>
  <% end %>

</main>

<footer>
  &copy; 2015
</footer>
